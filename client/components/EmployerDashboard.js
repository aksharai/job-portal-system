import React from "react";

const EmployerDashboard = () => {
  return (
    <div>
      <h2>Employer Dashboard</h2>
      <p>Feature: Post jobs (handled via backend seed API)</p>
    </div>
  );
};

export default EmployerDashboard;